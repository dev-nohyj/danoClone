import React from 'react';
import Header from '../components/Header';

function Main(props: { history: any }) {
    const { history } = props;
    return (
        <>
            <Header />
        </>
    );
}

export default Main;
